import { RealBrDirective } from './real-br.directive';

describe('RealBrDirective', () => {
  it('should create an instance', () => {
    const directive = new RealBrDirective();
    expect(directive).toBeTruthy();
  });
});
